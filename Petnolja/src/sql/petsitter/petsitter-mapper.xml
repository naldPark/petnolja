<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>

	<entry key="selectOldListCount">
		SELECT
    		COUNT(*) COUNT
    		FROM SITTER
    		JOIN MEMBER ON(SITTER_NO = MEM_NO)
    		WHERE PROMOTION_STATUS = 'Y'
	
	</entry>
	
	<entry key="selectNewListCount">
		SELECT
    		COUNT(*) COUNT
    		FROM SITTER
			JOIN MEMBER ON(SITTER_NO = MEM_NO)
	
	</entry>

	<entry key="selectOldPetsittereList">
		SELECT
    		MEM_ID
		    ,SITTER_NO
		    ,SITTER_ACCESS
		    ,PET_PERIOD
		    ,PET_NO
		    ,LICENSE
		    ,EXPERIENCE
		    ,MOTIVE
		    ,PROMOTION_STATUS
		    ,PROMOTION_DATE
		    ,STOP_REASON
		    ,STOP_CONTENT
		    ,PENALTY_COUNT
		    ,ADDITIONS
		FROM SITTER
		JOIN MEMBER ON(SITTER_NO = MEM_NO)
		WHERE PROMOTION_STATUS = 'Y'
		ORDER 
			BY SITTER.PROMOTION_DATE DESC
	</entry>
	
	<entry key="selectNewPetsittereList">
		SELECT
    		MEM_ID
		    ,SITTER_NO
		    ,SITTER_ACCESS
		    ,PET_PERIOD
		    ,PET_NO
		    ,LICENSE
		    ,EXPERIENCE
		    ,PROMOTION_STATUS
		    ,PROMOTION_DATE
		    ,ADDITIONS
		FROM SITTER
		JOIN MEMBER ON(SITTER_NO = MEM_NO)
		ORDER 
			BY SITTER.SITTER_NO DESC
	</entry>
	
	<entry key="selectNewPetsitter">
		SELECT
			SITTER_NO
    		,MEM_ID
		    ,PET_PERIOD
		    ,PET_NO
		    ,LICENSE
		    ,EXPERIENCE
		    ,MOTIVE
		    ,ADD_CONTENT
		    ,ADDITIONS
		FROM SITTER
		JOIN MEMBER ON(SITTER_NO = MEM_NO)
		ORDER 
			BY SITTER.SITTER_NO DESC
	</entry>
	
</properties>